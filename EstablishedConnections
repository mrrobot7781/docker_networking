import subprocess as sp

output = sp.getoutput("netstat -ant | grep ESTABLISHED")

count = {}

for line in output.split("\n"):

    ip = line.split()[4].split(":")[0]

    if ip in count:
        count[ip] += 1
    else:
        count[ip] = 1

for ip in count:
    print(ip, count[ip])
