import subprocess as sp

subnet = input("Enter Subnet: ")

for i in range(1,255):

    ip = subnet + "." + str(i)

    output = sp.getoutput(f"ping -c 1 {ip}")

    if "1 received" in output:

        print(ip,"Live")
